import 'package:flutter/material.dart';

const primaryColor = Color(0xFF7875B7);
//Color(0xFF7875B7)
//Color(0xFFf5dd61) - vàng
//Color(0xFF9a9b9d) - xám
const secondaryColor = Colors.white;

const disableColor = Color(0xFF9a9b9d);

const cancelColor = Colors.red;

const acceptColor = Colors.green;

const waitingColor = Colors.grey;

const normalTextStyle = TextStyle(color: secondaryColor, fontFamily: 'Open Sans');

const logoTextStyle = TextStyle(
  fontFamily: 'Open Sans',
  fontSize: 59,
  color: secondaryColor,
);

const buttonTextStyle = TextStyle(color: primaryColor, fontSize: 17, fontFamily: 'Open Sans');

const boldTextStyle = TextStyle(
  fontWeight: FontWeight.bold,
);

const dateTextStyle = TextStyle(
  fontFamily: 'Open Sans',
  fontSize: 12,
);

const disableText = TextStyle(
    fontSize: 17,
    fontFamily: 'Open Sans',
    color: disableColor);
